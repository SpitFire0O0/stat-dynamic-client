// Re-export generated OpenAPI types from the current project schema
// This lets us keep the example-style import paths (paths/components)
export type { paths, components, operations } from "../api/types";

